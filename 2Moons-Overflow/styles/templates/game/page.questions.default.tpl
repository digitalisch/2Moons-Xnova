{block name="title" prepend}{/block}{block name="content"}<div class="row"><div class="span9"><h3 class="spec">{$LNG.lm_faq}</h3><div class="tabbable"><div class="tab-content"><div class="tab-pane fade in active" id="tab1"><table class="redesign">	<tr>		<th>{$LNG.faq_overview}</th>	</tr>	<tr>		<td class="left">{foreach $LNG.questions as $categoryID => $categoryRow}<h2>{$categoryRow.category}</h2>		<ul>		{foreach $categoryRow as $questionID => $questionRow}		{if is_numeric($questionID)}			<li><a href="game.php?page=questions&amp;mode=single&amp;categoryID={$categoryID}&amp;questionID={$questionID}">{$questionRow.title}</a></li>		{/if}		{/foreach}		</ul>		{/foreach}</td>	</tr></table></div></div>{/block}{*{foreach $LNG.questions as $categoryID => $categoryRow}	<tr>		<th>{$categoryRow.category}</th>	</tr>	{foreach $categoryRow as $questionID => $questionRow}	{if is_numeric($questionID)}	<tr>		<th>{$questionRow.title}</th>	</tr>	<tr>		<td class="left">		{$questionRow.body}	</tr>	{/if}	{/foreach}	{/foreach}*}